import React from 'react';

// Define the Skills component
const Skills: React.FC = () => {
    return (
        <div>
         
        </div>
    );
};

// Export the Skills component for use in other parts of the app
export default Skills;
